{
  "name": "ai-score-to-song-platform",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "demo": "node dist/cli/demo.js",
    "test": "node --test dist/**/*.test.js",
    "check": "npm run build && npm run test",
    "start": "npm run build && node tools/serve.mjs",
    "serve": "node tools/serve.mjs",
    "smoke": "npm run build && node tools/smoke-server.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "@types/node": "^22.0.0"
  }
}
